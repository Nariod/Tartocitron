# Tartocitron with PInvoke

## PInvoke?
This is the default way, all API that are called by the malware are written in the IAT. AV and EDR can easily know what is going on in the executable statically.